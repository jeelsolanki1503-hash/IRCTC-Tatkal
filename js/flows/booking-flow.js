import { data } from '../mock-data.js';
import { getState, setState, holdRemaining } from '../state.js';
import { statusCard, timeLabel, toast } from '../ui.js';

/** Renders the reusable booking summary and four-stage progress UI. */
export function summary(stage = 0, showHold = false) { const steps = ['Availability', 'Details', 'Payment', 'Confirm']; const t = data.train; return `<aside class="summary-card"><div class="eyebrow">Your booking</div><ol class="stepper">${steps.map((step, i) => `<li class="${i < stage ? 'done' : ''} ${i === stage ? 'active' : ''}"><span>${i < stage ? '✓' : i + 1}</span>${step}</li>`).join('')}</ol>${showHold ? `<div class="seat-hold"><span>⏱ Seat held for you</span><strong data-hold>${timeLabel(holdRemaining())}</strong><small>Complete payment before this hold expires.</small></div>` : ''}<hr><h3>${t.name} <small>(${t.number})</small></h3><p class="muted">${t.route}</p><div class="journey-time"><b>${t.depart}</b><span>${t.duration}</span><b>${t.arrive}</b></div><p class="muted">${t.boarding} · ${data.search.category} · Tatkal</p></aside>`; }
/** Starts a live seat-hold countdown without allowing the UI to go blank. */
export function startHoldTimer() { const interval = setInterval(() => { document.querySelectorAll('[data-hold]').forEach(node => node.textContent = timeLabel(holdRemaining())); if (holdRemaining() === 0) { clearInterval(interval); toast('Seat hold expired in this demo. You can start a fresh booking.'); } }, 1000); }
/** Renders the fair-token request route. */
export function tokenView() { return `<main class="page two-column"><section class="content-card"><p class="eyebrow">Fair admission · Step 1 of 4</p><h1>Request your booking token</h1><p class="lead">Your place is determined by the queue, not by how fast you refresh. A token opens a short, protected booking window.</p>${getState().token ? statusCard('success', 'Token granted', 'Your token is reserved. Continue to passenger details now.', 'Token TFK-04812 · (DEMO)') : `<button class="button primary" data-request-token>Request token <span>→</span></button><p class="helper">One request per selected train. Your saved search remains available if tokens run out.</p>`}<section class="alternate"><h2>Other trains on this route</h2>${data.alternatives.map(train => `<div class="alternate-row"><span><b>${train.name}</b><small>${train.number} · ${train.time}</small></span><span class="token-count">${train.tokens ? `${train.tokens} tokens` : 'Sold out'}</span><button class="text-button" ${train.tokens ? '' : 'disabled'}>View</button></div>`).join('')}</section></section>${summary(0)}</main>`; }
/** Renders the wireframe-aligned token distribution screen with contextual guidance. */
export function tokenHighlightedView() { const t = data.train; return `<main class="page token-distribution-page"><a class="back-link" href="#/listing">‹ Back</a><div class="token-distribution-layout"><section class="token-distribution-main"><h1>Token Distribution</h1><p class="lead">Clicking on the button below will determine whether a booking token is available for this train.</p><div class="request-token-wrap"><button class="button primary" data-request-token>Request Token</button><aside class="token-request-tip" data-token-tip role="status"><span class="tip-arrow" aria-hidden="true">↑</span><button class="icon-button" type="button" data-close-token-tip aria-label="Close token guidance">×</button><p>You need to request a<br>token to book this train</p></aside></div><section class="same-route"><h2>Trains on the same route</h2>${data.alternatives.map(train => `<article class="same-route-card"><div><span class="train-number">${train.number}</span><h3>${train.name}</h3><div class="class-tags"><span>SL</span><span>3A</span><span>2A</span></div></div><div class="train-service-icons"><span>●</span><span>●</span></div><div class="station-block"><b>${train.time}</b><span>Wed, 27 Aug</span><span>Pune Junction</span></div><div class="route-duration"><span>18h 20m</span><i></i><small>+1 day</small></div><div class="station-block arrival"><b>05:30</b><span>Thu, 28 Aug</span><span>New Delhi</span></div><button class="button secondary compact" ${train.tokens ? '' : 'disabled'}>Check availability</button></article>`).join('')}<a class="button secondary" href="#/home">Select other route</a></section></section><aside class="token-train-summary"><ol class="stepper"><li class="active"><span>1</span>Availability</li><li><span>2</span>Details</li><li><span>3</span>Payment</li><li><span>4</span>Confirm</li></ol><div class="summary-heading"><span class="utility-icon">●</span><h2>Train Details</h2></div><hr><h3>${t.name} (${t.number})</h3><div class="summary-route"><div><b>${t.depart}</b><span>Wed, 27 Aug</span><span>Pune Junction</span></div><div class="route-duration"><span>${t.duration}</span><i></i><small>+1 day</small></div><div class="arrival"><b>${t.arrive}</b><span>Thu, 28 Aug</span><span>New Delhi</span></div></div><hr><dl><dt>Class</dt><dd>${data.search.category}</dd><dt>Boarding</dt><dd>${t.boarding}</dd><dt>Quota</dt><dd>${data.search.quota}</dd><dt>No. of seats</dt><dd>${data.search.seats}</dd></dl><a class="button secondary compact" href="#/home">Edit Details</a></aside></div></main><div class="token-highlight-backdrop" data-token-highlight-backdrop></div>`; }

/** Renders the granted-token state while preserving the selected train context. */
export function tokenReceivedView() { return tokenHighlightedView().replace(/<div class="request-token-wrap">[\s\S]*?<\/aside><\/div>/, `<div class="token-granted-row"><span class="button secondary">Token Granted</span><p><span class="granted-dot" aria-hidden="true"></span>Please wait while we redirect you to fill in your details in <b data-token-redirect>3s</b>.</p></div>`).replace('<div class="token-highlight-backdrop" data-token-highlight-backdrop></div>', ''); }

/** Selects the request or received state for the token route. */
export function tokenFlowView() { return getState().token ? tokenReceivedView() : tokenHighlightedView(); }

/** Binds the token request and delayed grant interaction. */
export function bindToken() { document.querySelector('[data-request-token]')?.addEventListener('click', () => { setState({ token: true, route: 'token-received', holdStarted: Date.now() }); toast('Token granted. Your protected booking window has begun.'); window.location.hash = '#/token-received'; }); }
/** Renders passenger details either as an editable form or saved review. */
export function detailsView() { const saved = getState().details; const p = data.passenger; return `<main class="page two-column"><section class="content-card"><p class="eyebrow">Passenger details · Step 2 of 4</p><h1>${saved ? 'Review your passenger details' : 'Who is travelling?'}</h1><p class="lead">We’ll save every completed step, so an interruption never makes you start over.</p>${saved ? `<article class="passenger-card"><span class="avatar">AR</span><div><b>1. ${p.name}</b><p>${p.age} years · ${p.gender} · ${p.berth} · ${p.meal}</p></div><button class="text-button" data-edit-details>Edit</button></article>${contactBlock(true)}<button class="button primary" data-continue-payment>Calculate fare & continue <span>→</span></button>` : `${passengerForm()}${contactBlock(false)}<button class="button primary" data-save-details>Save details & calculate fare <span>→</span></button>`}<details class="preferences"><summary>Other preferences (optional)</summary><label><input type="checkbox"> Consider for auto upgradation</label><label><input type="checkbox"> Add GST details</label></details></section>${summary(1, true)}</main>`; }
/** Renders the wireframe-aligned empty passenger and booking details screen. */
export function detailsWireframeView() { const t = data.train; return `<main class="page details-wireframe"><a class="back-link" href="#/token-request">‹ Back</a><p class="details-deadline"><span></span>You need to fill in the details and secure your seat in <b data-hold>${timeLabel(holdRemaining())}</b>.</p><div class="details-layout"><section class="details-main"><section class="details-section boarding-section"><h2>Change boarding station</h2><button class="boarding-choice" type="button"><b>${t.boarding}</b><span>Arrival: ${t.depart}</span><span>Departure: ${t.depart}</span><span>Day 1</span><span>Boarding Date: 28 Aug 2026</span><strong>⌄</strong></button></section><div class="details-heading"><h2><span class="section-dot"></span>Passenger Details</h2><div><button class="button secondary compact" type="button">Existing Passenger</button><button class="button primary compact" type="button">New Passenger</button></div></div><section class="details-section loyalty-card"><div class="loyalty-heading"><span class="section-dot"></span><div><h3>IRCTC Co-branded Card Benefits</h3><p>Earn more: 1% PG Waiver + 10% Reward Points</p></div></div><div class="loyalty-actions"><button type="button"><span></span>Earn Loyalty Points</button><button type="button"><span></span>Pay With Loyalty Points</button><button type="button"><span></span>Skip</button></div></section><section class="details-section contact-section"><p>Ticket details will be sent to <b>${data.user.email}</b><br>and registered mobile number ${data.user.phone.replace('+91 ', '')}</p><label class="mobile-entry"><span>+91 · IND</span><input aria-label="Mobile number" inputmode="tel" placeholder="Mobile Number" value="${data.user.phone.replace('+91 ', '')}"></label></section><section class="details-section preference-section"><h2><span class="section-dot"></span>Other Preferences <b>⌄</b></h2><label><input type="checkbox"> Consider for Auto Upgradation</label><label><input type="text" aria-label="Preferred coach number" placeholder="Preferred Coach No"></label><label><input type="text" aria-label="GST details" placeholder="GST Details (Optional)"></label></section><section class="payment-preview"><h2><span class="section-dot"></span>Payment Options</h2><label><span><b>Pay through Credit & Debit Cards/ Net Banking/ Wallet/ EMI/ UPI_CC</b><small>Convenience Fee: ₹30/- + GST</small></span><input type="radio" name="preview-payment"></label><label><span><b>Pay through BHIM/UPI</b><small>Convenience Fee: ₹30/- + GST</small></span><input type="radio" name="preview-payment"></label><button class="button primary" type="button" data-save-details>Calculate Fare</button></section></section><aside class="booking-review-panel"><ol class="stepper"><li class="done"><span>✓</span>Availability</li><li class="active"><span>2</span>Details</li><li><span>3</span>Payment</li><li><span>4</span>Confirm</li></ol><div class="summary-heading"><span class="utility-icon">●</span><h2>Booking Review</h2></div><section class="review-train"><h3>${t.name} (${t.number})</h3><div class="summary-route"><div><b>${t.depart}</b><span>Wed, 27 Aug</span><span>Pune Junction</span></div><div class="route-duration"><span>${t.duration}</span><i></i><small>+1 day</small></div><div class="arrival"><b>${t.arrive}</b><span>Thu, 28 Aug</span><span>New Delhi</span></div></div><dl><dt>Class</dt><dd>${data.search.category}</dd><dt>Boarding</dt><dd>${t.boarding}</dd><dt>Quota</dt><dd>${data.search.quota}</dd><dt>No. of seats</dt><dd>${data.search.seats}</dd></dl></section><section class="fare-preview"><h3>Fare Summary</h3><p><span>Ticket Fare</span><b>₹${t.fare.toLocaleString('en-IN')}</b></p><hr><p><span>Payable Amount<small>Inclusive of all taxes</small></span><b>₹${t.fare.toLocaleString('en-IN')}</b></p></section><a href="#/how-it-works">View Cancellation Policy</a></aside></div></main>`; }

/** Renders the calculated-fare state with completed passenger and fare details. */
export function detailsFilledView() { const state = getState(); const passengers = state.passengers.length ? state.passengers : [{ name: data.passenger.name, age: data.passenger.age, sex: data.passenger.gender, berth: data.passenger.berth }, { name: 'Srishti Mittal', age: '27', sex: 'Female', berth: 'Lower' }]; const rows = passengers.map((passenger, index) => `<article><b>${index + 1}. ${passenger.name}</b><span>${passenger.age} years · ${passenger.sex} · India · ${passenger.berth} berth</span></article>`).join(''); return detailsWireframeView().replace(/<div class="details-heading">[\s\S]*?<\/div><section class="details-section loyalty-card">/, `<div class="details-heading"><h2><span class="section-dot"></span>Passenger Details</h2></div><section class="filled-passenger-list">${rows}</section><section class="details-section loyalty-card">`).replace('<button class="button primary" type="button" data-save-details>Calculate Fare</button>', '<a class="details-edit-link" href="#/details" data-edit-filled>Edit details</a>').replace('<section class="fare-preview">', `<section class="review-passengers"><h3>Passenger Details</h3>${rows}</section><section class="fare-preview filled-fare">`).replace(`<p><span>Ticket Fare</span><b>₹${data.train.fare.toLocaleString('en-IN')}</b></p><hr><p><span>Payable Amount<small>Inclusive of all taxes</small></span><b>₹${data.train.fare.toLocaleString('en-IN')}</b></p>`, `<p><span>Ticket Fare</span><b>₹${data.train.fare.toLocaleString('en-IN')}</b></p><p><span>Convenience Fee</span><b>₹240</b></p><p><span>Travel Insurance</span><b>₹0.50</b></p><hr><p><span>Payable Amount<small>Inclusive of all taxes</small></span><b>₹${data.train.total.toLocaleString('en-IN')}</b></p>`).replace('</aside></div></main>', '<button class="button primary pay-book-button" type="button" data-pay-book>Pay and Book</button></aside></div></main>'); }

/** Selects the empty or fare-calculated Details screen. */
export function detailsFlowView() { return getState().details ? detailsFilledView() : detailsWireframeView(); }

/** Returns labelled passenger inputs to meet accessible form requirements. */
function passengerForm() { const p = data.passenger; return `<form class="form-grid" data-passenger-form><div><label for="passenger-name">Passenger name</label><input id="passenger-name" value="${p.name}" required></div><div><label for="passenger-age">Age</label><input id="passenger-age" type="number" value="${p.age}" required></div><div><label for="passenger-gender">Gender</label><select id="passenger-gender"><option>${p.gender}</option><option>Female</option><option>Other</option></select></div><div><label for="passenger-berth">Berth preference</label><select id="passenger-berth"><option>${p.berth}</option><option>Lower</option><option>Any</option></select></div></form>`; }
/** Returns the editable contact confirmation block. */
function contactBlock(saved) { return `<section class="contact-box"><h2>Ticket delivery</h2><p>Your ticket will be sent to <b>${data.user.email}</b> and your registered mobile.</p>${saved ? `<p class="contact-number">${data.user.phone} <button class="text-button">Edit</button></p>` : `<label for="phone">Registered mobile number</label><input id="phone" value="${data.user.phone}" inputmode="tel">`}</section>`; }
/** Connects the detail form to persisted progress and the payment route. */
export function bindDetails() { const save = () => { setState({ details: true, route: 'details-filled' }); toast('Fare calculated and passenger details saved.'); window.location.hash = '#/details-filled'; }; document.querySelector('[data-save-details]')?.addEventListener('click', save); document.querySelector('[data-continue-payment]')?.addEventListener('click', save); document.querySelector('[data-edit-details]')?.addEventListener('click', () => setState({ details: false })); startHoldTimer(); }
/** Renders the fee-transparent payment-method selection. */
export function paymentView() { const t = data.train; const fee = t.total - t.fare; return `<main class="page two-column"><section class="content-card"><p class="eyebrow">Payment · Step 3 of 4</p><h1>Choose a payment method</h1><div class="payable"><span>Total payable</span><strong>₹${t.total.toFixed(2)}</strong><small>Includes all charges · (DEMO)</small></div><fieldset class="payment-methods"><legend>Recommended</legend><label class="payment-choice checked"><input type="radio" name="method" checked> <span>UPI · Google Pay</span><small>₹0 convenience fee</small></label><label class="payment-choice"><input type="radio" name="method"> <span>IRCTC iPAY</span><small>₹0 convenience fee</small></label><label class="payment-choice"><input type="radio" name="method"> <span>Debit / Credit card</span><small>₹30 + GST</small></label></fieldset><details class="fare-details" open><summary>Fare breakdown</summary><p><span>Ticket fare</span><b>₹${t.fare.toLocaleString('en-IN')}.00</b></p><p><span>Convenience fee + GST</span><b>₹${fee.toFixed(2)}</b></p><p><span>Travel insurance</span><b>₹0.00</b></p></details><p class="terms">Tatkal tickets are non-refundable after confirmation. <a href="#/how-it-works">View cancellation policy</a></p><button class="button primary" data-pay>Pay & book securely <span>→</span></button></section>${summary(2, true)}</main>`; }
/** Renders the wireframe-aligned payment-method gateway selection screen. */
export function paymentGatewaySelectionView() { const t = data.train; const state = getState(); const passengers = state.passengers.length ? state.passengers : [{ name: data.passenger.name, age: data.passenger.age, sex: data.passenger.gender, berth: data.passenger.berth }]; const methods = [{ group: 'Multiple payment service', items: ['PhonePe', 'Google Pay', 'PayU', 'Paytm'] }, { group: 'UPI / BHIM / USSD', items: ['Paytm UPI', 'Amazon Pay'] }, { group: 'Net Banking', items: ['SBI', 'HDFC Bank', 'ICICI Bank', 'Axis Bank'] }, { group: 'Debit / Credit Cards', items: ['Visa', 'Mastercard', 'RuPay', 'American Express'] }]; return `<main class="page payment-gateway-selection"><a class="back-link" href="#/details-filled">‹ Back</a><div class="payment-gateway-layout"><section class="payment-method-panel"><header><h1><span class="section-dot">₹</span>Payment Method</h1><div><small>Payable Amount</small><strong>₹${t.total.toLocaleString('en-IN')}</strong></div></header><label class="ipay-choice"><input type="radio" name="gateway-method" checked><span class="payment-logo">i</span><span><b>IRCTC iPAY</b><small>Fast, secure checkout with cards, UPI, net banking, and wallets.</small></span></label>${methods.map(section => `<section class="method-group"><h2><span class="section-dot">${section.group.includes('UPI') ? '₹' : section.group.includes('Card') ? '▣' : '●'}</span>${section.group}</h2><div class="method-tiles">${section.items.map(item => `<div class="method-option"><span class="method-mark">${item.slice(0,2).toUpperCase()}</span><b>${item}</b></div>`).join('')}</div></section>`).join('')}</section><aside class="payment-review-panel"><ol class="stepper"><li class="done"><span>✓</span>Availability</li><li class="done"><span>✓</span>Details</li><li class="active"><span>3</span>Payment</li><li><span>4</span>Confirm</li></ol><div class="summary-heading"><span class="utility-icon">✓</span><h2>Booking Review</h2></div><section class="review-train"><h3>${t.name} (${t.number})</h3><div class="summary-route"><div><b>${t.depart}</b><span>Wed, 27 Aug</span><span>Pune Junction</span></div><div class="route-duration"><span>${t.duration}</span><i></i><small>+1 day</small></div><div class="arrival"><b>${t.arrive}</b><span>Thu, 28 Aug</span><span>New Delhi</span></div></div><dl><dt>Class</dt><dd>${data.search.category}</dd><dt>Boarding</dt><dd>${t.boarding}</dd><dt>Quota</dt><dd>${data.search.quota}</dd><dt>No. of seats</dt><dd>${data.search.seats}</dd></dl></section><section class="review-passengers"><h3>Passenger Details</h3>${passengers.map(passenger => `<article><b>${passenger.name}</b><span>${passenger.age} years · ${passenger.sex} · India · ${passenger.berth} berth</span></article>`).join('')}</section><section class="fare-preview filled-fare"><h3>Fare Summary</h3><p><span>Ticket Fare</span><b>₹${t.fare.toLocaleString('en-IN')}</b></p><p><span>Convenience Fee</span><b>₹240</b></p><p><span>Travel Insurance</span><b>₹0.50</b></p><hr><p><span>Payable Amount<small>Inclusive of all taxes</small></span><b>₹${t.total.toLocaleString('en-IN')}</b></p></section><a href="#/how-it-works">View Cancellation Policy</a><button class="button primary pay-book-button" type="button" data-pay>Pay and Book</button></aside></div></main>`; }

/** Starts the gateway handoff and saves a payment-in-progress state. */
export function bindPayment() { document.querySelector('[data-pay]')?.addEventListener('click', () => { setState({ payment: 'pending', route: 'gateway' }); window.location.hash = '#/gateway'; }); startHoldTimer(); }
/** Renders an explicit third-party payment handoff, including a demo QR visual. */
export function gatewayView() { return `<main class="payment-page-wireframe"><div class="payment-page-top"><a class="back-link" href="#/payment">‹ Back</a><p><span></span>This page will time out in <b data-gateway-time>05:43</b></p></div><div class="payment-page-layout"><aside class="payment-total-panel"><h1><span class="section-dot">₹</span>Payment Method</h1><button class="payment-total" type="button"><b>Total:</b><strong>₹${data.train.total.toFixed(2)}</strong><span>⌄</span></button><p class="payment-demo-note"><b>Demo payment</b><span>No real payment will be made. Choose an outcome below to continue the prototype.</span></p><div class="payment-demo-actions"><button class="button primary" type="button" data-payment-result="success">Successful payment</button><button class="button secondary" type="button" data-payment-result="pending">Pending payment</button><button class="button secondary payment-failed-action" type="button" data-payment-result="failed">Failed payment</button></div><footer>POWERED BY <span class="payment-logo">i</span></footer></aside><section class="payment-options-panel"><header><h2>Payment options</h2><a href="#/payment" aria-label="Close payment options">×</a></header><div class="payment-options-body"><nav><h3>UPI Payment</h3><button class="upi-option is-selected" type="button"><span class="section-dot">₹</span><span><b>UPI</b><small>Pay via UPI apps, number or ID</small></span></button></nav><section class="upi-qr-panel"><h2>Scan via any UPI app</h2><div class="upi-app-marks" aria-label="Supported UPI applications"><span>G</span><span>P</span><span>U</span><span>BH</span><span>PA</span></div><div class="qr-card"><span class="qr" aria-hidden="true"><i></i><i></i><i></i><i></i><i></i></span><span>This QR will expire in <b data-gateway-time>05:43</b></span></div></section></div></section></div></main>`; }
/** Routes the selected simulated gateway result to its unambiguous status. */
export function bindGateway() { document.querySelectorAll('[data-payment-result]').forEach(button => button.addEventListener('click', () => { const result = button.dataset.paymentResult; setState({ payment: result, route: 'status' }); window.location.hash = '#/status'; })); }
/** Renders the three canonical outcomes, including recovery for failed and pending states. */
export function statusView() { const payment = getState().payment || 'pending'; const outcomes = { success: { title: 'Payment confirmed', copy: 'Your seat is booked!', detail: 'Your seat booking details are mailed to you at the registered email id!', close: '#/confirmation', icon: '✓' }, failed: { title: 'Payment Failed', copy: 'Please retry paying by canceling this window.', detail: 'An error occurred.', close: '#/payment', icon: '×' }, pending: { title: 'Payment Pending', copy: 'Please wait.. Confirming with the bank..', detail: '', close: '#/gateway', icon: '…' } }; const outcome = outcomes[payment] || outcomes.pending; return `${gatewayView()}<div class="payment-outcome-backdrop"><section class="payment-outcome-modal payment-outcome-${payment}" role="dialog" aria-modal="true" aria-labelledby="payment-outcome-title"><a href="${outcome.close}" aria-label="Close payment status">×</a><span class="payment-outcome-icon">${outcome.icon}</span><h1 id="payment-outcome-title">${outcome.title}</h1><p>${outcome.copy}</p>${outcome.detail ? `<small>${outcome.detail}</small>` : ''}</section></div>`; }
/** Resolves the mock pending flow after a visible, bounded wait. */
export function bindStatus() { document.querySelector('[data-resolve-pending]')?.addEventListener('click', event => { event.currentTarget.disabled = true; event.currentTarget.textContent = 'Reconciling with bank…'; setTimeout(() => { setState({ payment: 'success' }); toast('Bank confirmation received. Your ticket is confirmed.'); window.location.hash = '#/confirmation'; }, 2200); }); }
/** Renders a clear confirmed-ticket receipt. */
export function confirmationView() { return `<main class="page confirmation"><section class="content-card ticket"><p class="eyebrow">Booking confirmed · Step 4 of 4</p>${statusCard('success', 'Your Tatkal ticket is confirmed', 'PNR ' + data.payment.pnr + ' · Booking reference ' + data.payment.reference, 'A confirmation has been sent to ' + data.user.email)}<div class="ticket-route"><div><small>FROM</small><b>Pune Junction</b><strong>${data.train.depart}</strong></div><span>→</span><div><small>TO</small><b>New Delhi</b><strong>${data.train.arrive}</strong></div></div><div class="ticket-details"><span>Train <b>${data.train.name} (${data.train.number})</b></span><span>Passenger <b>${data.passenger.name}</b></span><span>Class <b>AC 3 Tier · Tatkal</b></span><span>Coach / berth <b>Allotted at chart preparation</b></span></div><div class="status-actions"><button class="button primary" data-download>Download receipt</button><a class="button secondary" href="#/home">Book another ticket</a></div></section></main>`; }
/** Provides useful receipt feedback without pretending to generate a real ticket. */
export function bindConfirmation() { document.querySelector('[data-download]')?.addEventListener('click', () => toast('Demo receipt prepared. In production, this downloads the official e-ticket.')); }
