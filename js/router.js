/** Extracts the requested hash route, falling back to home. */
export function getRoute() { const route = window.location.hash.replace('#/', '') || 'home'; if (route === 'token-request' || route === 'token-received') return 'token'; return route === 'details-filled' ? 'details' : route; }
/** Starts hash routing and places every newly rendered screen at its top edge. */
export function startRouter(render) { if ('scrollRestoration' in history) history.scrollRestoration = 'manual'; const renderFromTop = async () => { await render(); requestAnimationFrame(() => { const highlightedTokenCta = window.matchMedia('(max-width: 767px)').matches && document.querySelector('.token-request-tip:not(.hidden)')?.closest('.request-token-wrap')?.querySelector('[data-request-token]'); if (highlightedTokenCta) highlightedTokenCta.scrollIntoView({ block: 'center', inline: 'nearest', behavior: 'auto' }); else window.scrollTo({ top: 0, left: 0, behavior: 'auto' }); }); }; window.addEventListener('hashchange', renderFromTop); renderFromTop(); }
